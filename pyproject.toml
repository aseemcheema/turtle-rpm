[project]
name = "turtle-rpm"
version = "0.1.0"
description = "Risk and portfolio management tool for the Turtle trading system"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.31.0",
]
